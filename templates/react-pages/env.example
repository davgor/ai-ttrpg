# Copy to repo root as `.env.example`. Real values go in `.env` (gitignored).

# Vite public base (override for user/org Pages site → /)
# VITE_BASE=/
# BASE=/

# Backend API origin for fetch clients (no trailing slash)
# VITE_API_URL=https://api.example.com
