# 005.3 — Host shell: IPC chat session + UI mount

Electron main owns the live session via orchestrator helpers; preload exposes a narrow API; renderer mounts `@ai-ttrpg/ui`.

## Acceptance criteria

- [ ] `App` mounts UI chat as the primary view
- [ ] Brand name remains visible in chat chrome
- [ ] Check-for-updates remains available from a minimal settings affordance
- [ ] Renderer does not import orchestrator/dm/npc (IPC only)
- [ ] Existing auto-update unit tests still pass; update banner still mounts
