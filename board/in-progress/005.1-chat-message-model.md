# 005.1 — Orchestrator chat model, demo seed, action formatting

Chat domain helpers live in `@ai-ttrpg/orchestrator` (not React). Display formatting for `*action*` italics may live in `@ai-ttrpg/ui`.

## Acceptance criteria

- [ ] `ChatMessage` / system events distinguish dialogue (dm | npc | player) vs system events
- [ ] Demo seed reproduces the mock conversation (enter NPCs, dialogue, leave NPCs)
- [ ] `appendPlayerMessage` / id factory live in orchestrator and are unit-tested
- [ ] Action/`*asterisk*` display parsing covered in UI package tests
