# 003.4 — CI package boundary enforcement

Add automated dependency barriers for the three core packages (orchestrator / dm / npc) and wire them into CI, per `docs/architecture/monorepo-packages.md`.

## Acceptance criteria

- [x] Three core packages scaffolded under `packages/` with narrow `exports`
- [x] `npm run boundaries` fails on forbidden cross-package imports (dm↔npc, orchestrator→agents, agent→orchestrator internals, agent→fs/sqlite/electron)
- [x] Unit tests cover clean graph + intentional violation fixtures
- [x] `pr-checks.yml` runs the boundaries check
- [x] Architecture doc marks CI barrier as in place
- [x] `npm run lint`, `npm test`, `npm run fireguard`, `npm run typecheck`, `npm run deadcode`, `npm run build` pass
